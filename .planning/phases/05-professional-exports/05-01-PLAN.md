---
phase: 05-professional-exports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/report-web/package.json, apps/report-web/components/export/HighlightCard.tsx, apps/report-web/lib/export/generateImage.ts, apps/report-web/components/report/ExportButtons.tsx]
autonomous: true
requirements: [REPO-03]
user_setup: []
must_haves:
  truths:
    - "User can download a PNG image containing match highlights"
    - "Highlight image uses a Bento Grid layout with match stats"
  artifacts:
    - path: "apps/report-web/components/export/HighlightCard.tsx"
      provides: "Social media graphic layout"
    - path: "apps/report-web/lib/export/generateImage.ts"
      provides: "Client-side image capture logic"
  key_links:
    - from: "apps/report-web/components/report/ExportButtons.tsx"
      to: "apps/report-web/lib/export/generateImage.ts"
      via: "button click handler"
---

<objective>
Implement the "Match Highlight" social media graphic generation (REPO-03).
This involves creating a visually appealing component that captures the essence of a match and providing a way to export it as a high-resolution image.

Purpose: Increase engagement by allowing users to share professional-looking match results on social media.
Output: HighlightCard component and image generation utility.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-professional-exports/05-RESEARCH.md
@apps/report-web/package.json
@apps/report-web/components/report/ExportButtons.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install html-to-image and setup directory</name>
  <files>apps/report-web/package.json</files>
  <action>
    - Install `html-to-image@1.11.11` in `apps/report-web`.
    - Create directory `apps/report-web/components/export/`.
  </action>
  <verify>
    <automated>grep "html-to-image" apps/report-web/package.json && ls -d apps/report-web/components/export/</automated>
  </verify>
  <done>Dependency installed and directory created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HighlightCard component</name>
  <files>apps/report-web/components/export/HighlightCard.tsx</files>
  <action>
    - Create a `HighlightCard` component that takes an `AdaptedMatch` report.
    - Implement a "Bento Grid" layout (per RESEARCH.md) using Tailwind CSS.
    - Sections should include: Match Result, Top Scorer, Best Bowler, and an InningsPro watermark.
    - Use "Premium" styling (gradients, glassmorphism, high contrast).
    - Ensure the component is rendered with a fixed aspect ratio (e.g., 4:5 for Instagram) and high-resolution padding.
  </action>
  <verify>
    <automated>ls apps/report-web/components/export/HighlightCard.tsx</automated>
  </verify>
  <done>HighlightCard component implemented with premium bento-grid styling.</done>
</task>

<task type="auto">
  <name>Task 3: Implement image generation utility and integration</name>
  <files>apps/report-web/lib/export/generateImage.ts, apps/report-web/components/report/ExportButtons.tsx</files>
  <action>
    - Create `apps/report-web/lib/export/generateImage.ts` using `html-to-image`.
    - The utility should accept a DOM reference and a filename, then trigger a download of the PNG.
    - Update `ExportButtons.tsx` to include a "Download Highlight" button.
    - In `ExportButtons.tsx`, render the `HighlightCard` (hidden or off-screen) to provide the DOM reference for capture.
    - Use `pixelRatio: 2` for high resolution as suggested in research.
  </action>
  <verify>
    <automated>grep "toPng" apps/report-web/lib/export/generateImage.ts && grep "Download Highlight" apps/report-web/components/report/ExportButtons.tsx</automated>
  </verify>
  <done>Image generation utility implemented and wired to the UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Browser Storage | Exported images are saved to local filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | HighlightCard | mitigate | Sanitize match data (team names, player names) before rendering to prevent XSS in captured image. |
</threat_model>

<verification>
- Verify that clicking "Download Highlight" triggers a PNG download.
- Open the downloaded PNG and check that it contains the Match Highlight graphic with correct data.
- Verify the layout is a Bento Grid as specified.
</verification>

<success_criteria>
- User can successfully download a high-resolution "Match Highlight" PNG.
- The graphic includes key match stats and professional branding.
</success_criteria>

<output>
After completion, create `.planning/phases/05-professional-exports/05-01-SUMMARY.md`
</output>

---
phase: 05-professional-exports
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified: [apps/report-web/components/export/ExportDialog.tsx, apps/report-web/components/report/ExportButtons.tsx]
autonomous: true
requirements: [REPO-02, REPO-03]
user_setup: []
must_haves:
  truths:
    - "User can access all export options through a single 'Export' button"
    - "Export options are presented in a visually appealing modal dialog"
  artifacts:
    - path: "apps/report-web/components/export/ExportDialog.tsx"
      provides: "Unified export selection UI"
  key_links:
    - from: "apps/report-web/components/report/ExportButtons.tsx"
      to: "apps/report-web/components/export/ExportDialog.tsx"
      via: "Dialog trigger"
---

<objective>
Unify all export formats (PDF, Image, JSON, Excel) into a single, professional "Export Center" dialog.
This improves the user experience by reducing clutter on the main report page and providing a clear, high-fidelity selection interface.

Purpose: Professionalize the export workflow and consolidate premium features.
Output: ExportDialog component and updated ExportButtons entry point.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/report-web/components/report/ExportButtons.tsx
@apps/report-web/components/export/HighlightCard.tsx
@apps/report-web/components/export/PrintLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ExportDialog component</name>
  <files>apps/report-web/components/export/ExportDialog.tsx</files>
  <action>
    - Create a `ExportDialog` component using `@radix-ui/react-dialog` (or the existing dialog pattern in the project).
    - The dialog should offer four distinct options: "Professional PDF", "Social Media Highlight", "Raw JSON Data", and "Excel Spreadsheet".
    - Use `framer-motion` for smooth entrance and exit animations.
    - Each option should have an icon (Lucide), a clear title, and a brief description.
    - Wire up the respective export functions (captured from props or context) to each option.
  </action>
  <verify>
    <automated>ls apps/report-web/components/export/ExportDialog.tsx</automated>
  </verify>
  <done>ExportDialog implemented with unified options and premium styling.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ExportButtons to use the new dialog</name>
  <files>apps/report-web/components/report/ExportButtons.tsx</files>
  <action>
    - Replace the individual buttons in `ExportButtons.tsx` with a single, prominent "Export Report" button (using `GlowButton` or similar premium UI component if available).
    - Trigger the `ExportDialog` when this button is clicked.
    - Ensure all necessary data (match report, raw JSON) and callbacks (onPdfExport) are passed correctly to the dialog.
    - Remove the redundant `HighlightCard` rendering from `ExportButtons` and move it into the `ExportDialog` or maintain it as a hidden child if needed for DOM capture.
  </action>
  <verify>
    <automated>grep "ExportDialog" apps/report-web/components/report/ExportButtons.tsx</automated>
  </verify>
  <done>ExportButtons refactored to use the central ExportDialog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI → Export Subsystems | Dialog centralizes access to multiple export mechanisms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | ExportDialog | mitigate | Ensure dialog doesn't trigger multiple heavy export processes simultaneously; disable trigger buttons while an export is in progress. |
</threat_model>

<verification>
- Click the "Export Report" button on the report page.
- Verify the `ExportDialog` opens with all four export options.
- Click each option and verify they still function correctly (PDF, PNG, JSON, Excel).
</verification>

<success_criteria>
- The export workflow is unified into a single, professional interface.
- All four export formats are accessible and functional from the new dialog.
</success_criteria>

<output>
After completion, create `.planning/phases/05-professional-exports/05-03-SUMMARY.md`
</output>

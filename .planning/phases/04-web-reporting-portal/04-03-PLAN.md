---
phase: "04-web-reporting-portal"
plan: "03"
type: "execute"
wave: 3
depends_on: ["04-02"]
files_modified:
  - "apps/report-web/components/report/InningsCharts.tsx"
  - "apps/report-web/components/report/ExportButtons.tsx"
  - "apps/report-web/lib/export/exportJson.ts"
autonomous: true
requirements: ["REPO-01", "REPO-04"]
must_haves:
  truths:
    - "The web portal displays Manhattan and Worm charts that match the match events."
    - "User can download a JSON file containing the raw match data."
  artifacts:
    - path: "apps/report-web/lib/export/exportJson.ts"
      provides: "Utility to download raw match data as JSON"
    - path: "apps/report-web/components/report/InningsCharts.tsx"
      provides: "Innings-specific visualizations (Manhattan/Worm)"
  key_links:
    - from: "apps/report-web/components/report/ExportButtons.tsx"
      to: "apps/report-web/lib/export/exportJson.ts"
      via: "download action"
---

<objective>
Complete the web reporting portal by adding analytical visualizations (Manhattan and Worm charts) and implementing the requirement to export raw match data as a machine-readable JSON format.

Purpose: Provide deeper insights into the match progression and enable data portability.
Output: Completed web reporting features with data viz and JSON export.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-web-reporting-portal/04-02-SUMMARY.md
@apps/report-web/components/report/InningsCharts.tsx
@apps/report-web/components/report/ExportButtons.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Manhattan and Worm charts</name>
  <files>apps/report-web/components/report/InningsCharts.tsx</files>
  <action>
    Implement ManhattanChart and WormChart components using Recharts. Derive the chart data from the match events (overs/runs per ball). Ensure consistent coloring and responsiveness. Manhattan should show runs per over, and Worm should show cumulative runs vs overs.
  </action>
  <verify>
    <automated>grep -q "ManhattanChart" apps/report-web/components/report/InningsCharts.tsx</automated>
  </verify>
  <done>Analytical charts are implemented and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Implement JSON data export</name>
  <files>apps/report-web/lib/export/exportJson.ts, apps/report-web/components/report/ExportButtons.tsx</files>
  <action>
    Create a utility `exportJson.ts` that takes the current match object and initiates a file download with a `.ipro` or `.json` extension. Wire up the "Download JSON" button in the ExportButtons component (Requirement: REPO-04).
  </action>
  <verify>
    <automated>ls apps/report-web/lib/export/exportJson.ts</automated>
  </verify>
  <done>JSON export functionality is implemented and wired to the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Final UI polish and responsiveness</name>
  <files>apps/report-web/components/report/ReportPageClient.tsx, apps/report-web/app/report/page.tsx</files>
  <action>
    Perform a final pass on the reporting UI. Ensure all sections are responsive, handle empty states (e.g. no match uploaded), and match the professional "Pro Max" design standards.
  </action>
  <verify>
    <automated>ls apps/report-web/app/report/page.tsx</automated>
  </verify>
  <done>Web portal UI is polished and ready for v1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | JSON Export | accept | The export contains the same data already displayed in the UI. No PII is included in the export. |
</threat_model>

<verification>
- Verify that Manhattan and Worm charts display correct data.
- Verify that clicking "Download JSON" triggers a file download with the correct match data.
</verification>

<success_criteria>
- Analytical charts provide a visual summary of the match.
- Raw JSON export is machine-readable and matches the input .ipro format.
</success_criteria>

<output>
After completion, create .planning/phases/04-web-reporting-portal/04-03-SUMMARY.md
</output>

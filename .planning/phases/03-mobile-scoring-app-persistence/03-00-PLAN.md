---
phase: 03-mobile-scoring-app-persistence
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [apps/mobile/package.json, apps/mobile/vitest.config.ts, apps/mobile/src/services/__tests__/db.service.test.ts]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Test runner is configured for Expo/React Native environment"
    - "Base integration tests for database services are established"
  artifacts:
    - path: "apps/mobile/vitest.config.ts"
      provides: "Vitest configuration"
    - path: "apps/mobile/src/services/__tests__/db.service.test.ts"
      provides: "Initial DB integration test"
---

<objective>
Setup the testing infrastructure for the mobile app to ensure "Nyquist Compliance" (automated verification of all requirements). This includes configuring Vitest and creating base integration tests for the persistence layer.

Purpose: Enable automated verification of STORE-01 and STORE-02.
Output: Configured test runner and initial test suite.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mobile-scoring-app-persistence/03-RESEARCH.md
@apps/mobile/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Vitest for Expo</name>
  <files>apps/mobile/package.json, apps/mobile/vitest.config.ts</files>
  <action>
    1. Add `vitest` and `@testing-library/react-native` to `devDependencies` in `apps/mobile/package.json`.
    2. Create `apps/mobile/vitest.config.ts` with appropriate aliases and React Native presets.
    3. Add a `test` script to `package.json`.
  </action>
  <verify>
    <automated>npm test --prefix apps/mobile -- --version</automated>
  </verify>
  <done>Vitest configured and runnable.</done>
</task>

<task type="auto">
  <name>Task 2: Create initial integration and unit tests</name>
  <files>apps/mobile/src/services/__tests__/db.service.test.ts, apps/mobile/src/services/__tests__/export.service.test.ts, apps/mobile/src/features/scoring/store/useScoringStore.test.ts</files>
  <action>
    Create initial test files to enable Nyquist compliance for Phase 03:
    1. `apps/mobile/src/services/__tests__/db.service.test.ts`: Verify `DatabaseService.addBallEvent` and `getBallEventsByMatch`.
    2. `apps/mobile/src/services/__tests__/export.service.test.ts`: Verify `ExportService.generateExport` matches `.ipro` schema (Wave 3).
    3. `apps/mobile/src/features/scoring/store/useScoringStore.test.ts`: Verify `loadMatch`, `recordEvent`, and `undo` via mocked database (Wave 2).
    For Wave 0, these can be placeholder tests that fail or skip until the services are updated in later waves, but the structure must exist.
  </action>
  <verify>
    <automated>npm test --prefix apps/mobile</automated>
  </verify>
  <done>Initial test suite structure established.</done>
</task>

</tasks>

<verification>
Run `npm test` in `apps/mobile` to ensure the environment is ready for subsequent waves.
</verification>

<success_criteria>
- `npm test` successfully executes in the mobile app directory.
- Database service can be tested in an automated fashion.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-scoring-app-persistence/03-00-SUMMARY.md`
</output>

---
phase: 01-match-engine-core-rules
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - packages/match-engine/src/selectors.ts
  - packages/match-engine/src/__tests__/selectors.test.ts
autonomous: true
requirements: [FORMAT-01]
must_haves:
  truths:
    - "Selectors correctly calculate total score from event stream"
    - "Batter and Bowler statistics match official scoring rules"
    - "Match ends correctly for a 5-over / 5-wicket configuration"
  artifacts:
    - path: "packages/match-engine/src/selectors.ts"
      provides: "Derived state calculations"
  key_links:
    - from: "packages/match-engine/src/selectors.ts"
      to: "packages/match-engine/src/reducer.ts"
      via: "State structure"
---

<objective>
Implement derived state selectors to calculate scorecards and statistics from the event stream. Verify the engine's configurability for specific match formats.

Purpose: Provide a clear view of match progress and statistics.
Output: Set of pure functional selectors and verification of 5-over match format.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-match-engine-core-rules/01-02-SUMMARY.md
@packages/shared-types/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Scorecard and Stats Selectors</name>
  <files>packages/match-engine/src/selectors.ts, packages/match-engine/src/__tests__/selectors.test.ts</files>
  <read_first>
    - packages/shared-types/src/index.ts
    - packages/match-engine/src/reducer.ts
  </read_first>
  <action>
    - Create `packages/match-engine/src/selectors.ts`.
    - Implement the following selectors (pure functions):
      - `getInningsScore(innings: Innings)`: Returns `{ totalRuns: number, totalWickets: number, oversBowled: string }`.
        - `oversBowled` should be in "X.Y" format (e.g., "5.2").
      - `getBatterStats(innings: Innings, batterId: string)`: Returns `{ runs: number, balls: number, fours: number, sixes: number, strikeRate: number }`.
      - `getBowlerStats(innings: Innings, bowlerId: string)`: Returns `{ overs: string, maidens: number, runs: number, wickets: number, economy: number }`.
    - Create `packages/match-engine/src/__tests__/selectors.test.ts`.
    - Mock a `Match` with various event types (deliveries, boundaries, extras, wickets) and verify the selectors return correct values.
  </action>
  <verify>
    <automated>cd packages/match-engine && node --test src/__tests__/selectors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `getInningsScore` correctly aggregates runs and wickets.
    - `getBatterStats` correctly counts boundaries and calculates strike rate.
    - `getBowlerStats` correctly calculates economy rate.
    - Selectors are pure functions and do not mutate state.
  </acceptance_criteria>
  <done>Core scorecard and statistics selectors implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Verify 5-over / 5-player Match Format</name>
  <files>packages/match-engine/src/__tests__/selectors.test.ts</files>
  <read_first>
    - packages/shared-types/src/index.ts (MatchRules)
    - packages/match-engine/src/reducer.ts
  </read_first>
  <action>
    - Add a specific test case in `selectors.test.ts` for a "Corporate" format:
      - `MatchRules`: `maxOversPerInnings: 5`, `ballsPerOver: 6`, `wicketsPerInnings: 5`.
    - Simulate a sequence of events reaching 5.0 overs.
    - Verify `getInningsScore` returns "5.0" overs.
    - Verify (via a `getMatchStatus` selector or similar) that the match is considered finished.
    - Simulate 5 wickets falling.
    - Verify the innings is considered finished.
  </action>
  <verify>
    <automated>cd packages/match-engine && node --test src/__tests__/selectors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test case for 5-over limit passes.
    - Test case for 5-wicket limit passes.
    - All calculations use the `MatchRules` provided in the state, not hardcoded values.
  </acceptance_criteria>
  <done>Configurability for 5-over match format verified (FORMAT-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reporting | Statistics calculations must be accurate for professional reporting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03-01 | Information Disclosure | Selectors | accept | Logic is pure and only calculates based on provided state. |
</threat_model>

<verification>
Ensure that `getInningsScore` handles the transition between overs (e.g. 0.5 -> 1.0) correctly and that `ballsBowled % 6` logic is consistent with the `MatchRules`.
</verification>

<success_criteria>
- All selectors produce correct outputs for varied event streams.
- 5-over/5-wicket format is fully supported and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-match-engine-core-rules/01-03-SUMMARY.md`
</output>

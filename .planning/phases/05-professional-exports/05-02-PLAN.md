---
phase: 05-professional-exports
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [apps/report-web/components/export/PrintLayout.tsx, apps/report-web/components/report/ReportPageClient.tsx, apps/report-web/lib/export/exportPdf.ts]
autonomous: true
requirements: [REPO-02]
user_setup: []
must_haves:
  truths:
    - "User can download a professional PDF summary of the match"
    - "PDF output follows a formal A4 layout, optimized for printing"
  artifacts:
    - path: "apps/report-web/components/export/PrintLayout.tsx"
      provides: "Formal A4-optimized scorecard layout"
  key_links:
    - from: "apps/report-web/components/report/ReportPageClient.tsx"
      to: "apps/report-web/components/export/PrintLayout.tsx"
      via: "rendering for react-to-print"
---

<objective>
Implement the "Professional PDF Summary" export (REPO-02).
This involves creating a dedicated layout for printing that differs from the web dashboard, ensuring it looks formal and fits perfectly on A4 pages.

Purpose: Provide a professional, printable record of the match for leagues and teams.
Output: PrintLayout component and refined PDF export logic.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-professional-exports/05-RESEARCH.md
@apps/report-web/components/report/ReportPageClient.tsx
@apps/report-web/lib/export/exportPdf.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PrintLayout component</name>
  <files>apps/report-web/components/export/PrintLayout.tsx</files>
  <action>
    - Create a `PrintLayout` component in `apps/report-web/components/export/`.
    - Design it specifically for A4 printing (clean white background, sharp typography, minimal shadows).
    - Include full batting/bowling scorecards for both innings.
    - Use `break-inside: avoid` and `page-break-before: always` CSS utilities (as suggested in RESEARCH.md) to ensure clean transitions between sections/innings.
    - Add a professional header with match metadata and InningsPro logo/branding.
  </action>
  <verify>
    <automated>ls apps/report-web/components/export/PrintLayout.tsx</automated>
  </verify>
  <done>PrintLayout component implemented with A4 optimizations.</done>
</task>

<task type="auto">
  <name>Task 2: Update ReportPageClient for dedicated print ref</name>
  <files>apps/report-web/components/report/ReportPageClient.tsx</files>
  <action>
    - Import `PrintLayout` into `ReportPageClient.tsx`.
    - Create a new `printRef` using `useRef<HTMLDivElement>(null)`.
    - Render `PrintLayout` inside a hidden container (e.g., `className="hidden print:block"` or absolute position off-screen).
    - Pass `printRef` to `useExportPdf` instead of the general `componentRef`.
    - This ensures that when the user clicks "Download PDF", it prints the `PrintLayout` instead of the dashboard view.
  </action>
  <verify>
    <automated>grep "PrintLayout" apps/report-web/components/report/ReportPageClient.tsx</automated>
  </verify>
  <done>ReportPageClient updated to use a dedicated print reference.</done>
</task>

<task type="auto">
  <name>Task 3: Refine PDF export utility and CSS</name>
  <files>apps/report-web/lib/export/exportPdf.ts</files>
  <action>
    - Refine `exportPdf.ts` if needed to ensure `react-to-print` options are optimal (e.g., `removeAfterPrint: true`).
    - Add a global or component-level print style block to ensure background colors are printed (using `-webkit-print-color-adjust: exact`) and margins are handled.
  </action>
  <verify>
    <automated>grep "useReactToPrint" apps/report-web/lib/export/exportPdf.ts</automated>
  </verify>
  <done>PDF export logic and print CSS refined for maximum fidelity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Browser Print Service | DOM content is sent to browser print subsystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | PrintLayout | accept | Ensure no sensitive developer-only metadata is rendered in the print-only component. |
</threat_model>

<verification>
- Click "Download PDF" in the web portal.
- Verify the print preview (or downloaded file) shows the A4-optimized `PrintLayout` instead of the web dashboard.
- Check for clean page breaks between innings.
</verification>

<success_criteria>
- User can successfully download a professional A4-formatted PDF.
- The PDF contains high-fidelity scorecards with professional branding.
</success_criteria>

<output>
After completion, create `.planning/phases/05-professional-exports/05-02-SUMMARY.md`
</output>

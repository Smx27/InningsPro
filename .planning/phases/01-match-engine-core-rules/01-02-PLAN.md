---
phase: 01-match-engine-core-rules
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/match-engine/src/reducer.ts
  - packages/match-engine/src/__tests__/reducer.test.ts
autonomous: true
requirements: [RULE-01, RULE-02, FORMAT-03]
must_haves:
  truths:
    - "RECORD_DELIVERY updates runs and increments legal ball count"
    - "RECORD_EXTRA (Wide/No-ball) adds 1 run and does not increment legal ball count"
    - "Striker rotates on odd runs and over completion"
    - "Same bowler cannot bowl consecutive overs"
  artifacts:
    - path: "packages/match-engine/src/reducer.ts"
      provides: "Core scoring logic"
  key_links:
    - from: "packages/match-engine/src/reducer.ts"
      to: "packages/shared-types"
      via: "BallEvent interface"
---

<objective>
Implement the pure functional reducer for cricket scoring, covering basic deliveries, extras, wickets, and over transitions.

Purpose: Core engine logic for deterministic scoring.
Output: Completed reducer with 100% test coverage for basic scoring scenarios.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-match-engine-core-rules/01-01-SUMMARY.md
@packages/shared-types/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Delivery recording and Strike Rotation</name>
  <files>packages/match-engine/src/reducer.ts, packages/match-engine/src/__tests__/reducer.test.ts</files>
  <read_first>
    - packages/match-engine/src/types.ts
    - packages/shared-types/src/index.ts
    - apps/mobile/src/services/match-engine.service.ts (for reference implementation of formatOvers/calculateRunRate)
  </read_first>
  <action>
    - Update `matchReducer` in `packages/match-engine/src/reducer.ts` to handle `RECORD_DELIVERY`.
    - Logic for `RECORD_DELIVERY`:
      - Identify active innings: `state.innings[state.innings.length - 1]`.
      - Create `DeliveryBallEvent`:
        - `id`: Unique string (e.g., `event_${timestamp}`).
        - `kind`: 'delivery'.
        - `runsOffBat`: from action.
        - `isBoundary`: from action.
        - `batterId`, `nonStrikerId`, `bowlerId`: from action.
        - `overNumber`: Calculated from previous events (legal balls / ballsPerOver).
        - `ballInOver`: Count of events in current over + 1.
      - Update `innings.events` by appending the new event.
      - Handle Strike Rotation: If `runsOffBat` is 1, 3, or 5, the next action should have `batterId` and `nonStrikerId` swapped (this logic is typically external to the *stored* state but the engine can facilitate it by returning a state that Selectors use).
    - Add tests to `reducer.test.ts` for scoring 1 run and verifying strike rotation.
  </action>
  <verify>
    <automated>cd packages/match-engine && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `matchReducer` appends a `DeliveryBallEvent` to the latest innings.
    - Test for "1 run swaps strike" passes.
    - Test for "4 runs (boundary) does not swap strike" passes.
  </acceptance_criteria>
  <done>Basic delivery recording and strike rotation implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Extras and Rebowling Logic</name>
  <files>packages/match-engine/src/reducer.ts, packages/match-engine/src/__tests__/reducer.test.ts</files>
  <read_first>
    - packages/match-engine/src/reducer.ts
  </read_first>
  <action>
    - Implement `RECORD_EXTRA` in `matchReducer`.
    - Logic for `RECORD_EXTRA`:
      - If `wide` or `no-ball`:
        - Add 1 run (penalty) + any runs in payload.
        - `rebowled: true`.
        - These balls do NOT count as legal balls for `overNumber` calculation.
      - If `bye` or `leg-bye`:
        - Add runs.
        - `rebowled: false`.
        - These balls DO count as legal balls.
    - Add tests for `wide` (1 run, same over count) and `no-ball` (1 run + runs, same over count).
  </action>
  <verify>
    <automated>cd packages/match-engine && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `RECORD_EXTRA` with `wide` adds 1 run but does not increment legal ball count in `MatchRules`.
    - `RECORD_EXTRA` with `bye` increments legal ball count.
    - Tests for all 4 extra types pass.
  </acceptance_criteria>
  <done>Extras logic and rebowling rules implemented (FORMAT-03).</done>
</task>

<task type="auto">
  <name>Task 3: Implement Wickets and Over Completion</name>
  <files>packages/match-engine/src/reducer.ts, packages/match-engine/src/__tests__/reducer.test.ts</files>
  <read_first>
    - packages/match-engine/src/reducer.ts
  </read_first>
  <action>
    - Implement `RECORD_WICKET` in `matchReducer`.
    - Implement Over Completion:
      - If current event is a legal ball AND `legalBalls % rules.ballsPerOver === 0`:
        - Striker and Non-Striker MUST swap for the next over.
    - Implement Bowler Rotation Check (RULE-02):
      - If `RECORD_DELIVERY`/`EXTRA` is the first ball of a new over AND `bowlerId` is the same as the last over's bowler, return `state` unchanged (D-03).
    - Implement Match Completion Check:
      - If total wickets == `rules.wicketsPerInnings` OR legal balls == `maxOvers * ballsPerOver`:
        - Set `match.status = 'completed'`.
    - Add tests for over completion swap and bowler rotation violation.
  </action>
  <verify>
    <automated>cd packages/match-engine && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Wickets are recorded correctly in the event stream.
    - Striker swaps after the 6th legal ball.
    - Engine rejects (returns original state) if the same bowler tries to bowl two consecutive overs.
  </acceptance_criteria>
  <done>Wickets, over transitions, and match completion logic implemented (RULE-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Match Logic | Bowler rotation and over completion are enforced by the engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02-01 | Integrity | matchReducer | mitigate | Enforce non-consecutive bowler rule at the reducer level. |
</threat_model>

<verification>
Run the full test suite in `packages/match-engine` to ensure all scoring edge cases (including rebowled extras at the end of an over) are handled correctly.
</verification>

<success_criteria>
- Reducer handles all core ball events.
- MCC/Corporate rotation rules enforced.
- Test coverage for all requirement-relevant scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/01-match-engine-core-rules/01-02-SUMMARY.md`
</output>

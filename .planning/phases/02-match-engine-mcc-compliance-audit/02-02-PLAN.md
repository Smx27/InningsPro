---
phase: "02-match-engine-mcc-compliance-audit"
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["packages/match-engine/src/types.ts", "packages/match-engine/src/reducer.ts", "packages/match-engine/src/__tests__/mcc-laws.test.ts"]
autonomous: true
requirements: ["RULE-03", "RULE-04", "RULE-05"]
must_haves:
  truths:
    - "New batter always takes strike after a 'Caught' dismissal (MCC 2022 Law 18.11)"
    - "5-run penalty runs can be recorded between balls"
    - "Batter can be retired as 'Retired Hurt' or 'Retired Out'"
  artifacts:
    - path: "packages/match-engine/src/types.ts"
      provides: "New actions: RECORD_PENALTY, RETIRE_BATTER"
    - path: "packages/match-engine/src/reducer.ts"
      provides: "New logic for penalties, retirements, and Law 18.11"
  key_links:
    - from: "packages/match-engine/src/reducer.ts"
      to: "packages/shared-types/src/index.ts"
      via: "PenaltyBallEvent"
---

<objective>
Incorporate MCC 2022 Law 18.11 (Caught dismissal) and implement standalone penalty runs and batter retirements.

Purpose: Compliance with professional cricket rules and support for complex match events.
Output: Updated reducer with penalty and retirement support, and strict 2022 law enforcement.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/match-engine/src/reducer.ts
@packages/match-engine/src/types.ts
@packages/shared-types/src/index.ts
@/data/data/com.termux/files/home/applications/InningsPro/.planning/phases/02-match-engine-mcc-compliance-audit/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Types with Penalty and Retirement Actions</name>
  <files>packages/match-engine/src/types.ts</files>
  <action>
    Add `RECORD_PENALTY` and `RETIRE_BATTER` to `MatchEngineAction`.
    - `RECORD_PENALTY`: payload includes `awardedTo` (batting/bowling), `reason`, `id`, `timestamp`.
    - `RETIRE_BATTER`: payload includes `batterId`, `retirementType` ('hurt' | 'out'), `id`, `timestamp`.
    Note: `Retired Hurt` is returnable, `Retired Out` is final.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Types updated with new actions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Laws and New Actions in Reducer</name>
  <files>packages/match-engine/src/reducer.ts</files>
  <action>
    1. Update `RECORD_WICKET` logic: If `wicketType === 'caught'`, ensure the new batter always takes strike (no rotation based on runs completed/crossing) as per MCC 2022 Law 18.11.
    2. Implement `RECORD_PENALTY`: Insert a `PenaltyBallEvent` (already defined in shared-types) into the event stream. Penalty runs are always 5.
    3. Implement `RETIRE_BATTER`:
       - If `out`: Count as a wicket (add a `wicket` event with `retired-out` type).
       - If `hurt`: Just record the event/state change (batter is absent but not out). You may need to track 'active' batters in the state.
    4. Update `recreateStateFromStream` to handle the new action mappings.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Reducer implemented for penalties, retirements, and MCC 2022 laws.</done>
</task>

<task type="auto">
  <name>Task 3: Test MCC Laws and Penalties</name>
  <files>packages/match-engine/src/__tests__/mcc-laws.test.ts</files>
  <action>
    Create `packages/match-engine/src/__tests__/mcc-laws.test.ts`:
    - Test that 'Caught' wickets always result in the new batter taking strike even if 1 run was completed.
    - Test that 5-run penalties are correctly added to totals for the appropriate team.
    - Test 'Retired Out' correctly ends an innings if no more batters are available.
    - Test 'Retired Hurt' leaves the batter available to return later (verify state).
  </action>
  <verify>
    <automated>npm test -- packages/match-engine/src/__tests__/mcc-laws.test.ts</automated>
  </verify>
  <done>Comprehensive tests pass for MCC laws, penalties, and retirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Scoring Input → Reducer | User might try to retire a batter who is not on the field. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | RETIRE_BATTER | mitigate | Reducer validates that the retired batter is one of the active batters (striker/non-striker). |
</threat_model>

<verification>
Ensure all tests in `packages/match-engine` pass, particularly the new law-specific ones.
</verification>

<success_criteria>
- Engine enforces MCC 2022 Law 18.11 correctly.
- Penalties and retirements are functional and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/02-match-engine-mcc-compliance-audit/02-02-SUMMARY.md`
</output>

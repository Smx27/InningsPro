---
phase: 03-mobile-scoring-app-persistence
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [apps/mobile/src/services/match-engine.service.ts, apps/mobile/src/features/scoring/store/useScoringStore.ts, apps/mobile/src/features/scoring/components/RunGrid.tsx]
autonomous: true
requirements: [STORE-01, RULE-01, RULE-02]
must_haves:
  truths:
    - "Scoring state is derived from @inningspro/match-engine selectors"
    - "Match state is correctly reconstructed from database events on app load"
  artifacts:
    - path: "apps/mobile/src/services/match-engine.service.ts"
      provides: "Match Engine integration wrapper"
    - path: "apps/mobile/src/features/scoring/store/useScoringStore.ts"
      provides: "Event-sourced Zustand store"
  key_links:
    - from: "apps/mobile/src/features/scoring/store/useScoringStore.ts"
      to: "@inningspro/match-engine"
      via: "matchReducer and recreateStateFromStream"
---

<objective>
Integrate the shared `@inningspro/match-engine` into the mobile app to ensure consistent scoring rules and enable event-sourced state management. This plan replaces existing placeholder logic with the engine and implements the hydration flow from SQLite.

Purpose: Enforce MCC laws and ensure state reliability via event-sourcing.
Output: Engine-integrated scoring store and services.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mobile-scoring-app-persistence/03-RESEARCH.md
@packages/match-engine/src/index.ts
@apps/mobile/src/services/db.service.ts
@apps/mobile/src/features/scoring/store/useScoringStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MatchEngineService</name>
  <files>apps/mobile/src/services/match-engine.service.ts</files>
  <action>
    Create a service that wraps `@inningspro/match-engine` to:
    1. Provide `processAction(state, action)` which calls `matchReducer` and returns the next state.
    2. Provide `hydrateMatch(matchId, rules, events)` which calls `recreateStateFromStream`.
    3. Include helper functions to map engine state to UI-friendly selectors (using `match-engine` selectors).
  </action>
  <verify>
    <automated>npm run typecheck --prefix apps/mobile</automated>
  </verify>
  <done>MatchEngineService implemented and exported.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor useScoringStore</name>
  <files>apps/mobile/src/features/scoring/store/useScoringStore.ts</files>
  <action>
    Refactor the Zustand store to:
    1. Hold the `MatchEngineState` as the primary state.
    2. Implement `loadMatch(matchId)`: Fetch match info and events from `DatabaseService`, then use `MatchEngineService.hydrateMatch` to set state.
    3. Implement `recordEvent(action)`: 
       - Calculate next state using `MatchEngineService.processAction`.
       - If successful, persist the event to SQLite via `DatabaseService.addBallEvent`.
       - Update store state.
    4. Implement `undo()`: 
       - Remove last event from DB via `DatabaseService.undoLastBall`.
       - Re-hydrate store from remaining events.
  </action>
  <verify>
    <automated># Manual verification in UI recommended in next plan</automated>
  </verify>
  <done>useScoringStore refactored for event-sourcing.</done>
</task>

<task type="auto">
  <name>Task 3: Update Scoring UI components</name>
  <files>apps/mobile/src/features/scoring/components/RunGrid.tsx</files>
  <action>
    Update `RunGrid` and other core scoring components to:
    1. Consume state from the new `useScoringStore`.
    2. Dispatch engine-compatible actions (e.g., `RECORD_DELIVERY`, `RECORD_EXTRA`) instead of manual state updates.
    3. Handle the `lastRejectionReason` from the engine to show alerts (e.g., consecutive overs from same bowler).
  </action>
  <verify>
    <automated>npm run typecheck --prefix apps/mobile</automated>
  </verify>
  <done>UI components wired to the new scoring store.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Store -> Engine | UI actions are processed through the immutable engine reducer. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | apps/mobile/src/features/scoring/store/useScoringStore.ts | mitigate | State is managed centrally in Zustand; all transitions are dictated by the shared match-engine logic. |
</threat_model>

<verification>
- Load a match and verify it hydrates correctly from existing DB events.
- Record various events (runs, wickets) and verify they persist and reflect in the UI immediately.
- Test "Undo" functionality and verify it correctly swaps strikers and reverts scores.
</verification>

<success_criteria>
- Scoring UI displays data derived from the Match Engine state.
- Real-time scoring correctly enforces rules (like bowler rotation).
- Match state survives app restarts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-scoring-app-persistence/03-02-SUMMARY.md`
</output>

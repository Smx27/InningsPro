---
phase: "04-web-reporting-portal"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "apps/report-web/package.json"
  - "apps/report-web/lib/match-adapter/index.ts"
  - "apps/report-web/lib/store.ts"
  - "apps/report-web/components/upload/UploadCard.tsx"
autonomous: true
requirements: ["REPO-01"]
must_haves:
  truths:
    - "The web portal can parse a valid .ipro file using the shared export-schema."
    - "The parsed match data is stored in the application state."
  artifacts:
    - path: "apps/report-web/lib/match-adapter/index.ts"
      provides: "Adapter for .ipro schema parsing"
    - path: "apps/report-web/lib/store.ts"
      provides: "Zustand store for match report state"
  key_links:
    - from: "apps/report-web/components/upload/UploadCard.tsx"
      to: "apps/report-web/lib/match-adapter/index.ts"
      via: "parseMatchExport call"
---

<objective>
Integrate the shared @inningspro/export-schema and @inningspro/match-engine into the web portal and implement a data adapter for .ipro file parsing. This forms the foundation for visualizing real match data instead of dummy reports.

Purpose: Ensure the web portal uses the same data contracts and logic as the mobile scoring app.
Output: Working upload logic that validates and stores .ipro match data.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-web-reporting-portal/04-RESEARCH.md
@apps/report-web/package.json
@apps/report-web/lib/store.ts
@packages/export-schema/src/v1/schemas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared package dependencies</name>
  <files>apps/report-web/package.json</files>
  <action>
    Update apps/report-web/package.json to include "@inningspro/export-schema": "workspace:*" and "@inningspro/match-engine": "workspace:*" in dependencies. Run pnpm install to link them.
  </action>
  <verify>
    <automated>grep -q "@inningspro/export-schema" apps/report-web/package.json && grep -q "@inningspro/match-engine" apps/report-web/package.json</automated>
  </verify>
  <done>Dependencies added and linked.</done>
</task>

<task type="auto">
  <name>Task 2: Implement MatchAdapter for .ipro parsing</name>
  <files>apps/report-web/lib/match-adapter/index.ts</files>
  <action>
    Create an adapter that uses `v1.parseMatchExport` from `@inningspro/export-schema` to validate and parse uploaded JSON data. The adapter should return a structured Match object that the UI components can consume. Handle Zod validation errors gracefully.
  </action>
  <verify>
    <automated>ls apps/report-web/lib/match-adapter/index.ts</automated>
  </verify>
  <done>Adapter implemented and exports parsing function.</done>
</task>

<task type="auto">
  <name>Task 3: Update store and upload logic</name>
  <files>apps/report-web/lib/store.ts, apps/report-web/components/upload/UploadCard.tsx</files>
  <action>
    1. Update `useReportStore` in `apps/report-web/lib/store.ts` to use the `Match` type from `@inningspro/export-schema` (or the adapter's output type) instead of the local `MatchReport`.
    2. Update `UploadCard.tsx` to use the new adapter for file parsing. Ensure that when a file is uploaded, it is parsed, validated, and then set in the store. Redirect the user to the report page on success.
  </action>
  <verify>
    <automated>grep -q "parseMatchExport" apps/report-web/components/upload/UploadCard.tsx</automated>
  </verify>
  <done>Store updated and UploadCard integrated with the new adapter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File Input → Parser | Untrusted user-provided JSON file enters the application logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | File Upload | mitigate | Use Zod schema validation (from @inningspro/export-schema) to ensure only valid match data is processed. |
| T-04-01-02 | Denial of Service | File Upload | mitigate | Implement basic file size limits and JSON parse depth checks if necessary. |
</threat_model>

<verification>
- Verify that @inningspro/export-schema and @inningspro/match-engine are available in apps/report-web.
- Verify that a valid .ipro file can be parsed without errors in the UI.
</verification>

<success_criteria>
- .ipro files are validated against the shared schema.
- Uploaded data is accessible in the report store.
</success_criteria>

<output>
After completion, create .planning/phases/04-web-reporting-portal/04-01-SUMMARY.md
</output>

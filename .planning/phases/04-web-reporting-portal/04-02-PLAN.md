---
phase: "04-web-reporting-portal"
plan: "02"
type: "execute"
wave: 2
depends_on: ["04-01"]
files_modified:
  - "apps/report-web/components/report/MatchHeader.tsx"
  - "apps/report-web/components/report/InningsSection.tsx"
  - "apps/report-web/components/report/BattingScorecard.tsx"
  - "apps/report-web/components/report/BowlingScorecard.tsx"
autonomous: false
requirements: ["REPO-01"]
must_haves:
  truths:
    - "The web portal displays accurate batting stats (runs, balls, 4s, 6s) calculated from match events."
    - "The web portal displays accurate bowling stats (overs, runs, wickets) calculated from match events."
    - "The scorecard layout matches the Corporate Turf format requirements (e.g. 5 players/overs)."
  artifacts:
    - path: "apps/report-web/components/report/BattingScorecard.tsx"
      provides: "Batting stats table using match-engine selectors"
    - path: "apps/report-web/components/report/BowlingScorecard.tsx"
      provides: "Bowling stats table using match-engine selectors"
  key_links:
    - from: "apps/report-web/components/report/InningsSection.tsx"
      to: "@inningspro/match-engine"
      via: "getBatterStats and getBowlerStats calls"
---

<objective>
Implement the full scorecard visualization by updating the existing report components to use the @inningspro/match-engine selectors. This ensures that the web portal calculates statistics identically to the mobile scoring app.

Purpose: Provide a professional, accurate summary of the match.
Output: Responsive and interactive scorecard UI.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-web-reporting-portal/04-01-SUMMARY.md
@apps/report-web/components/report/InningsSection.tsx
@packages/match-engine/src/selectors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MatchHeader and ScoreSummary</name>
  <files>apps/report-web/components/report/MatchHeader.tsx, apps/report-web/components/report/ScoreSummary.tsx</files>
  <action>
    Update MatchHeader and ScoreSummary components to read from the new Match object. Display team names, total scores, overs, and match result/status based on the shared schema data.
  </action>
  <verify>
    <automated>grep -q "match" apps/report-web/components/report/MatchHeader.tsx</automated>
  </verify>
  <done>Match header displays real data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement stats-driven BattingScorecard</name>
  <files>apps/report-web/components/report/BattingScorecard.tsx</files>
  <action>
    Refactor BattingScorecard.tsx to accept an Innings object. Use `getBatterStats` from `@inningspro/match-engine` to calculate stats for each player who batted in the innings. Map these stats to the table rows. Handle "not out" and dismissal strings.
  </action>
  <verify>
    <automated>grep -q "getBatterStats" apps/report-web/components/report/BattingScorecard.tsx</automated>
  </verify>
  <done>Batting scorecard uses match-engine selectors.</done>
</task>

<task type="auto">
  <name>Task 3: Implement stats-driven BowlingScorecard</name>
  <files>apps/report-web/components/report/BowlingScorecard.tsx</files>
  <action>
    Refactor BowlingScorecard.tsx to accept an Innings object. Use `getBowlerStats` from `@inningspro/match-engine` to calculate stats for each player who bowled in the innings. Handle over formatting (e.g. 2.4 overs).
  </action>
  <verify>
    <automated>grep -q "getBowlerStats" apps/report-web/components/report/BowlingScorecard.tsx</automated>
  </verify>
  <done>Bowling scorecard uses match-engine selectors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Scorecard UI</what-built>
  <how-to-verify>
    1. Start the web app (npm run dev).
    2. Upload a valid .ipro file (you can use one generated from Phase 3 or a mock).
    3. Verify that the scorecard displays all innings correctly.
    4. Check that the totals (runs, wickets, overs) match the expectations.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Implementation focuses on internal data presentation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | Scorecard | accept | The report is public by design; no sensitive PII is included in the .ipro schema by default. |
</threat_model>

<verification>
- Scorecard displays accurate stats derived from match-engine.
- Components handle multiple innings correctly.
</verification>

<success_criteria>
- All batting and bowling summaries are visible and accurate.
- UI handles variations in player counts and over limits (Corporate format).
</success_criteria>

<output>
After completion, create .planning/phases/04-web-reporting-portal/04-02-SUMMARY.md
</output>

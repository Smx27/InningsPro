---
phase: 03-mobile-scoring-app-persistence
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [apps/mobile/src/services/export.service.ts, apps/mobile/src/services/share.service.ts, apps/mobile/src/features/scoring/components/RunGrid.tsx]
autonomous: false
requirements: [STORE-02]
must_haves:
  truths:
    - "User can export a match as a .ipro JSON file via the native sharing dialog"
    - "Exported file validates against the @inningspro/export-schema"
    - "Buttons provide haptic feedback upon successful scoring"
  artifacts:
    - path: "apps/mobile/src/services/export.service.ts"
      provides: ".ipro generation logic"
    - path: "apps/mobile/src/services/share.service.ts"
      provides: "Native sharing integration"
  key_links:
    - from: "apps/mobile/src/services/export.service.ts"
      to: "@inningspro/export-schema"
      via: "Zod validation"
---

<objective>
Enable match portability by implementing the `.ipro` export feature. This plan also adds UX refinements like haptic feedback and double-tap prevention to ensure a professional and reliable scoring experience.

Purpose: Allow match data to be moved to the web portal and improve scorer confidence.
Output: Export/Sharing services and UX refinements.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mobile-scoring-app-persistence/03-RESEARCH.md
@packages/export-schema/src/index.ts
@apps/mobile/src/services/db.service.ts
@apps/mobile/src/features/scoring/store/useScoringStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ExportService</name>
  <files>apps/mobile/src/services/export.service.ts</files>
  <action>
    Implement `ExportService` to:
    1. Fetch full match data (match, teams, players, innings, ball events) from `DatabaseService`.
    2. Construct the `ExportSchemaV1` payload.
    3. Validate the payload using `parseMatchExport` from `@inningspro/export-schema`.
    4. Return the validated JSON string.
  </action>
  <verify>
    <automated>npm run typecheck --prefix apps/mobile</automated>
  </verify>
  <done>ExportService correctly generates validated .ipro JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ShareService</name>
  <files>apps/mobile/src/services/share.service.ts</files>
  <action>
    Implement `ShareService` to:
    1. Save the exported JSON to a temporary file using `Expo-File-System`.
    2. Open the native sharing dialog using `Expo-Sharing`.
    3. Ensure temporary files are cleaned up or managed appropriately.
  </action>
  <verify>
    <automated>npm run typecheck --prefix apps/mobile</automated>
  </verify>
  <done>ShareService enables exporting match files via native share sheet.</done>
</task>

<task type="auto">
  <name>Task 3: Add UX Polish (Haptics & Prevention)</name>
  <files>apps/mobile/src/features/scoring/components/RunGrid.tsx, apps/mobile/src/features/scoring/store/useScoringStore.ts</files>
  <action>
    1. Add `isProcessing` flag to `useScoringStore` to disable scoring buttons while a ball is being persisted (Pitfall 2).
    2. Integrate `expo-haptics` in `RunGrid` and other action handlers to provide tactile feedback on button press.
    3. Ensure "Undo" also triggers haptic feedback.
  </action>
  <verify>
    <automated>npm run typecheck --prefix apps/mobile</automated>
  </verify>
  <done>UX polish implemented for scoring actions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Phase 03 functionality: Persistence, Engine Integration, and Export.</what-built>
  <how-to-verify>
    1. Open the mobile app.
    2. Start a match and score several balls (verify haptic feedback).
    3. Force-close the app and reopen (verify match state persists).
    4. Perform an "Undo" (verify state reverts correctly).
    5. Go to Match Summary and trigger "Export Match" (verify .ipro file is generated and share sheet opens).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App -> OS Share | Exported files are handed off to the mobile operating system. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | apps/mobile/src/services/export.service.ts | mitigate | Ensure exported JSON only contains match-related data and no sensitive PII or system secrets. |
</threat_model>

<verification>
- Export a match and inspect the JSON to ensure it matches the `.ipro` schema version 1.0.0.
- Verify haptics work on physical device (simulator might not support).
</verification>

<success_criteria>
- Reliable offline scoring with persistence.
- Full MCC law enforcement via shared engine.
- Portable match exports via `.ipro` files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-scoring-app-persistence/03-03-SUMMARY.md`
</output>

---
phase: 03-mobile-scoring-app-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/mobile/package.json, apps/mobile/src/core/database/schema.ts, apps/mobile/src/services/db.service.ts]
autonomous: true
requirements: [STORE-01]
must_haves:
  truths:
    - "SQLite schema supports all BallEvent kinds (delivery, wicket, extra, penalty, retirement)"
    - "DatabaseService can persist and retrieve events using the new schema"
  artifacts:
    - path: "apps/mobile/src/core/database/schema.ts"
      provides: "Updated BallEvent table definition"
    - path: "apps/mobile/src/services/db.service.ts"
      provides: "Updated event persistence methods"
  key_links:
    - from: "apps/mobile/src/services/db.service.ts"
      to: "@inningspro/shared-types"
      via: "BallEvent type mapping"
---

<objective>
Refactor the mobile app's persistence layer to support the event-sourced scoring model. This involves updating the SQLite schema to align with the shared `BallEvent` structure and updating the database service to handle these detailed events.

Purpose: Ensure the local database can store all data required by the Match Engine to reconstruct state perfectly.
Output: Updated database schema and service.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mobile-scoring-app-persistence/03-RESEARCH.md
@packages/shared-types/src/index.ts
@apps/mobile/src/core/database/schema.ts
@apps/mobile/src/services/db.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dependencies and schema</name>
  <files>apps/mobile/package.json, apps/mobile/src/core/database/schema.ts</files>
  <action>
    1. Add workspace dependencies to `apps/mobile/package.json`: `@inningspro/match-engine`, `@inningspro/shared-types`, `@inningspro/export-schema`.
    2. Add `drizzle-kit` to `devDependencies`.
    3. Update `ball_events` table in `apps/mobile/src/core/database/schema.ts` to include fields for the discriminated union in `shared-types`:
       - `kind`: text (delivery, wicket, extra, penalty, retirement)
       - `timestamp`: text (ISO string)
       - `inningsId`: text
       - `runsOffBat`: integer
       - `isBoundary`: boolean
       - `playerOutId`: text
       - `creditedToBowler`: boolean
       - `runsCompleted`: integer
       - `extraType`: text
       - `rebowled`: boolean
       - `awardedTo`: text
       - `reason`: text
       - `retirementType`: text
    4. Keep `innings_number`, `over_number`, `ball_number` for easy querying but note they are derived from engine logic.
  </action>
  <verify>
    <automated>npm run typecheck --prefix apps/mobile</automated>
  </verify>
  <done>Schema updated and typechecking passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update DatabaseService</name>
  <files>apps/mobile/src/services/db.service.ts</files>
  <action>
    1. Update `addBallEvent` to accept the new `BallEvent` fields.
    2. Update `getBallEventsByMatch` to return events in chronological order, mapping the flat DB rows back to the `BallEvent` union types from `@inningspro/shared-types`.
    3. Ensure `invalidateCaches` works with the new structure.
    4. (Optional) Deprecate manual calculation methods like `getTotalRuns` as they will be handled by the engine's selectors in the next plan.
  </action>
  <verify>
    <automated># No tests exist yet, Wave 0 must create them or rely on typecheck for now</automated>
  </verify>
  <done>DatabaseService updated to support the new event-sourced schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> SQLite | Scorer actions are persisted to local storage. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | apps/mobile/src/services/db.service.ts | mitigate | Drizzle ORM uses parameterized queries for all operations. |
| T-03-02 | Availability | apps/mobile/src/core/database/schema.ts | mitigate | Use SQLite transactions in the next plan to ensure event log integrity. |
</threat_model>

<verification>
- Run `npm run typecheck` in `apps/mobile` to ensure schema changes don't break existing code.
- Manually verify `BallEvent` mapping in `DatabaseService` handles all kinds correctly.
</verification>

<success_criteria>
- `ball_events` table definition includes all fields from `shared-types` BallEvent.
- `DatabaseService.addBallEvent` persists detailed event data.
- `DatabaseService.getBallEventsByMatch` returns correctly mapped `BallEvent` objects.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-scoring-app-persistence/03-01-SUMMARY.md`
</output>

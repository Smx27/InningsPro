---
phase: 01-match-engine-core-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/match-engine/package.json
  - packages/match-engine/tsconfig.json
  - packages/match-engine/src/types.ts
  - packages/match-engine/src/index.ts
  - packages/match-engine/src/__tests__/reducer.test.ts
autonomous: true
requirements: [RULE-01]
must_haves:
  truths:
    - "@inningspro/match-engine is a valid workspace package"
    - "MatchEngineAction covers all core scoring events (runs, extras, wickets)"
    - "Reducer test suite is ready for TDD"
  artifacts:
    - path: "packages/match-engine/package.json"
      provides: "Package definition"
    - path: "packages/match-engine/src/types.ts"
      provides: "Engine-specific types and actions"
  key_links:
    - from: "packages/match-engine/package.json"
      to: "packages/shared-types"
      via: "pnpm workspace dependency"
---

<objective>
Initialize the shared match-engine package and define the core action types that will drive the scoring logic.

Purpose: Provide the infrastructure for the shared scoring engine.
Output: Valid monorepo package with defined actions and a test scaffold.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@packages/shared-types/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize match-engine package</name>
  <files>packages/match-engine/package.json, packages/match-engine/tsconfig.json</files>
  <read_first>
    - package.json (root)
    - pnpm-workspace.yaml
    - tsconfig.base.json
    - packages/shared-types/package.json
  </read_first>
  <action>
    - Create directory `packages/match-engine`.
    - Create `packages/match-engine/package.json` with:
      - name: "@inningspro/match-engine"
      - version: "0.1.0"
      - type: "module"
      - exports: { ".": "./src/index.ts" }
      - dependencies: { "@inningspro/shared-types": "workspace:*", "zod": "3.24.2" }
      - devDependencies: { "typescript": "5.7.3", "@types/node": "20.11.20" }
      - scripts: { "test": "node --test src/**/*.test.ts" }
    - Create `packages/match-engine/tsconfig.json` extending `../../tsconfig.base.json` with:
      - compilerOptions.outDir: "dist"
      - include: ["src/**/*"]
    - Run `pnpm install` from root to link dependencies.
  </action>
  <verify>
    <automated>ls packages/match-engine/package.json && cd packages/match-engine && pnpm list</automated>
  </verify>
  <acceptance_criteria>
    - `packages/match-engine/package.json` exists and contains `@inningspro/match-engine`.
    - `packages/match-engine/package.json` has `workspace:*` dependency on `@inningspro/shared-types`.
    - `pnpm install` completes successfully.
  </acceptance_criteria>
  <done>Package initialized and linked in the monorepo.</done>
</task>

<task type="auto">
  <name>Task 2: Define Actions and State types</name>
  <files>packages/match-engine/src/types.ts, packages/match-engine/src/index.ts</files>
  <read_first>
    - packages/shared-types/src/index.ts (source of truth for Match, BallEvent)
  </read_first>
  <action>
    - Create `packages/match-engine/src/types.ts`.
    - Define `MatchEngineAction` as a discriminated union:
      - `START_MATCH`: `{ matchId: string, rules: MatchRules }`
      - `RECORD_DELIVERY`: `{ runsOffBat: number, isBoundary: boolean, batterId: string, nonStrikerId: string, bowlerId: string }`
      - `RECORD_EXTRA`: `{ type: 'wide' | 'no-ball' | 'bye' | 'leg-bye', runs: number, batterId: string, nonStrikerId: string, bowlerId: string }`
      - `RECORD_WICKET`: `{ type: WicketBallEvent['wicketType'], playerOutId: string, batterId: string, nonStrikerId: string, bowlerId: string }`
    - Define `MatchEngineState` as an alias for `Match` from `@inningspro/shared-types`.
    - Export these types from `packages/match-engine/src/index.ts`.
  </action>
  <verify>
    <automated>grep -r "type MatchEngineAction" packages/match-engine/src/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/match-engine/src/types.ts` contains a exported `MatchEngineAction` type.
    - All 4 action types (START_MATCH, RECORD_DELIVERY, RECORD_EXTRA, RECORD_WICKET) are defined.
    - `MatchEngineState` is exported.
  </acceptance_criteria>
  <done>Types defined and exported from the package.</done>
</task>

<task type="auto">
  <name>Task 3: Create Test Scaffold</name>
  <files>packages/match-engine/src/__tests__/reducer.test.ts, packages/match-engine/src/reducer.ts</files>
  <read_first>
    - packages/match-engine/package.json (test script)
  </read_first>
  <action>
    - Create `packages/match-engine/src/reducer.ts` with a skeleton `matchReducer` that returns state unchanged for unknown actions.
    - Create `packages/match-engine/src/__tests__/reducer.test.ts`.
    - Write a basic test:
      - Initialize a mock `Match` object.
      - Call `matchReducer(mockMatch, { type: 'UNKNOWN_ACTION' as any })`.
      - Assert that the returned object matches the input.
    - Run the test suite.
  </action>
  <verify>
    <automated>cd packages/match-engine && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/match-engine/src/reducer.ts` exists and exports `matchReducer`.
    - `npm test` runs and passes.
  </acceptance_criteria>
  <done>Test suite is operational and verify the skeleton reducer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller -> Reducer | Reducer must handle malformed actions without crashing (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01-01 | Tampering | matchReducer | mitigate | Use TypeScript and Zod (future) to ensure internal state remains consistent. |
</threat_model>

<verification>
Verify that the `packages/match-engine` can be built and that the test suite runs from both the package directory and the monorepo root (via turbo if configured).
</verification>

<success_criteria>
- Package directory exists.
- `MatchEngineAction` type covers all requirements.
- Test suite is green with the skeleton reducer.
</success_criteria>

<output>
After completion, create `.planning/phases/01-match-engine-core-rules/01-01-SUMMARY.md`
</output>

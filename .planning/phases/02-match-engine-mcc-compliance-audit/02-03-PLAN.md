---
phase: "02-match-engine-mcc-compliance-audit"
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: ["packages/shared-types/src/index.ts", "packages/match-engine/src/reducer.ts", "packages/match-engine/src/selectors.ts", "packages/match-engine/src/__tests__/lms-logic.test.ts"]
autonomous: true
requirements: ["FORMAT-02", "RULE-05"]
must_haves:
  truths:
    - "Last batter remains on strike for the remainder of the over in LMS mode"
    - "Retired hurt batters are suggested as returnable options when a wicket falls"
  artifacts:
    - path: "packages/shared-types/src/index.ts"
      provides: "MatchRules.lastManStands field"
    - path: "packages/match-engine/src/selectors.ts"
      provides: "getMatchHints with LMS and returnable support"
  key_links:
    - from: "packages/match-engine/src/selectors.ts"
      to: "packages/match-engine/src/reducer.ts"
      via: "State consumption"
---

<objective>
Implement 'Last Man Standing' (LMS) strike rotation logic and expand match hints to support Specialized formats and returnable batters.

Purpose: Support corporate turf formats and professional batter management.
Output: Specialized rotation logic and enhanced AI suggestions for the scoring UI.
</objective>

<execution_context>
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/workflows/execute-plan.md
@/data/data/com.termux/files/home/applications/InningsPro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/match-engine/src/reducer.ts
@packages/match-engine/src/selectors.ts
@packages/shared-types/src/index.ts
@/data/data/com.termux/files/home/applications/InningsPro/.planning/phases/02-match-engine-mcc-compliance-audit/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LMS Rule and Implement Rotation Logic</name>
  <files>packages/shared-types/src/index.ts, packages/match-engine/src/reducer.ts</files>
  <action>
    1. Add `lastManStands: boolean` to `MatchRules` in `packages/shared-types/src/index.ts`.
    2. Update `matchReducer` to handle LMS strike rotation:
       - If `rules.lastManStands` is true AND only one batter remains (all others are out or retired-out), the striker stays on strike for all subsequent balls in the over.
       - Standard rotation on even runs/over completion is disabled for the last batter.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>LMS rule added and rotation logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Match Hints for LMS and Returns</name>
  <files>packages/match-engine/src/selectors.ts</files>
  <action>
    Update `getMatchHints` in `selectors.ts`:
    1. Handle LMS: If in LMS mode and one batter remains, suggest the same batter as the striker for the next ball regardless of runs.
    2. Handle Retirements: Include 'Retired Hurt' batters in the `suggestedStrikerId`/`suggestedNonStrikerId` hint list when a new batter is needed (after a wicket).
    3. Ensure `overJustCompleted` and bowler suggestions still function correctly in LMS mode.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Match hints accurately reflect LMS logic and returnable batter options.</done>
</task>

<task type="auto">
  <name>Task 3: Test LMS and Batter Returns</name>
  <files>packages/match-engine/src/__tests__/lms-logic.test.ts</files>
  <action>
    Create `packages/match-engine/src/__tests__/lms-logic.test.ts`:
    - Test LMS rotation: Verify last batter stays on strike for 6 balls of an over despite scoring singles.
    - Test Retirement return: Verify 'Retired Hurt' batter is suggested after a wicket falls.
    - Test that innings ends only when no more batters (including retired hurt) can take the field.
  </action>
  <verify>
    <automated>npm test -- packages/match-engine/src/__tests__/lms-logic.test.ts</automated>
  </verify>
  <done>Comprehensive tests pass for LMS and batter management.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Hint Logic → UI | Incorrect hints could lead to user error during high-speed scoring. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | getMatchHints | accept | Hints are non-binding suggestions; user can always override. |
</threat_model>

<verification>
Verify all match-engine tests pass.
</verification>

<success_criteria>
- LMS strike rotation works as specified.
- 'Retired Hurt' batters can return to the field via match hints.
</success_criteria>

<output>
After completion, create `.planning/phases/02-match-engine-mcc-compliance-audit/02-03-SUMMARY.md`
</output>

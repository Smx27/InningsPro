import { describe, it, expect, vi } from 'vitest';
// import { exportService } from '../export.service';

describe('ExportService', () => {
  it('placeholder for STORE-02: should generate .ipro export matching schema', async () => {
    // This will be implemented in a subsequent wave
    expect(true).toBe(true);
  });

  it('placeholder for STORE-02: should include match and events in export', async () => {
    // This will be implemented in a subsequent wave
    expect(true).toBe(true);
  });
});
